; generated by drom from package skeleton 'driver'
(library
  (name main)
  (public_name mazout.ppx_deriving)
  (kind ppx_deriver)
  (libraries ppxlib ppx_deriving caqti-lwt caqti-driver-sqlite3 caqti base alcotest mazout.runtime)
  (preprocess (pps ppxlib.metaquot))
  
  )


(rule
    (targets version.ml)
    (deps (:script version.mlt) package.toml)
    (action (with-stdout-to %{targets} (run %{ocaml} unix.cma %{script}))))

(documentation
  (package mazout.ppx_deriving))



